<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 配置全局属性 -->
    <settings>
        <!-- 使用jdbc的getGeneratedKeys获取数据库自增主键值 -->
        <setting name="useGeneratedKeys" value="true"/>

        <!-- 使用列别名替换列名 默认：true -->
        <setting name="useColumnLabel" value="true"/>

        <!-- 开启驼峰命名转换：Table{create_time} -> Entity{createTime} -->
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>

        <!-- 配置类型别名(它只和XML配置有关，存在的意义仅在于用来减少类完全限定名的冗余) -->
        <!--typeAliases>
            <typeAlias type="com.iclsi.entity.User" alias="User" />
            <typeAlias type="com.iclsi.entity.Clock" alias="Clock" />
            <typeAlias type="com.iclsi.entity.UserClock" alias="UserClock" />
            <typeAlias type="com.iclsi.dao.UserDao" alias="UserDao" />
            <typeAlias type="com.iclsi.dao.ClockDao" alias="ClockDao" />
            <typeAlias type="com.iclsi.dao.UserClockDao" alias="UserClockDao" />
        </typeAliases-->
</configuration>